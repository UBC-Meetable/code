type UserProfile @model @auth(rules: [{ allow: owner }]) @key(fields: ["email"]) {
  email: String!
  firstName: String
  lastName: String
  profilePicture: String
  bio: String
  userState: UserState
  university: String
  major: String
}

enum UserState {
  SIGNED_UP
  UNI_SELECTED
  DONE
}