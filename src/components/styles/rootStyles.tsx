import { StyleSheet } from "react-native";

const rootStyles = StyleSheet.create({
  root: {
    flex: 1,
    display: "flex",
    height: "100%",
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#0000",
    marginTop: -50,
    paddingTop: 0,
  },
});

export default rootStyles.root;
