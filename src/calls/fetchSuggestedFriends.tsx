import { GraphQLResult } from "@aws-amplify/api";
import { API } from "aws-amplify";

//listUsersQuery -> the lambda function they wrote
const fetchSuggestedFriends = () => {
  console.log("test");
};

export default fetchSuggestedFriends;
